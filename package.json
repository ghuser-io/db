{
  "name": "@ghuser/db-scripts",
  "version": "0.0.2",
  "description": "Edit .../data/**/*.json",
  "scripts": {
    "gentoc": "for md in $(git grep -l \"<\\!-- toc -->\"); do npx markdown-toc -i $md; done",
    "test": "mdspell '**/*.md' '!**/node_modules/**/*.md' --ignore-acronyms --no-suggestions --en-us"
  },
  "dependencies": {
    "@ghuser/github-contribs": "^2.2.2",
    "await-sleep": "0.0.1",
    "fetch-retry": "^1.2.1",
    "github-colors": "^2.2.17",
    "meow": "^5.0.0",
    "ora": "^2.0.0"
  },
  "devDependencies": {
    "all-contributors-cli": "^5.4.0",
    "markdown-spellcheck": "^1.3.1",
    "markdown-toc": "^1.2.0"
  }
}
